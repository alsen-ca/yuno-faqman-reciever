// Return a list of Tags. By default the first 10.
// So one writes allTags(1) and it returns the first 10 tags
// One writes allTags(2) and it returns the 11-20 Tags
func allTags(w http.ResponseWriter, r *http.Request, which number) {
	var qa Qa

	// Get the first 10 Tags.
	// If number != 1, then offsets by that number. Ignores 10*which
	
	// 40x if no answers
	// 200 if answers.

	return status_code, answers
}
